'use client';

export function PromotionRewardsSection() {
  return (
    <section className="mt-5 rounded-3xl bg-white p-5 shadow-xl">
      <p className="text-sm font-black uppercase text-[#FF6B00]">Promotion Rewards</p>
      <p className="mt-2 text-sm font-bold text-stone-600">
        Reward orchestration and reward configuration will migrate into this section in future PRs.
      </p>
    </section>
  );
}
