# E2E Tests

This folder contains Playwright end-to-end tests for SpinBite.

Run locally after installing Playwright:

```bash
npm run test:e2e
```

Set `PLAYWRIGHT_BASE_URL` to test a deployed preview environment instead of starting the local development server.
